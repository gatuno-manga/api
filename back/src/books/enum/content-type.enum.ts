/**
 * Tipo de conteúdo do capítulo
 * - IMAGE: Páginas de imagem (mangás, manhwas, scans)
 * - TEXT: Texto puro (novels, markdown, HTML)
 * - DOCUMENT: Arquivo de documento (PDF, EPUB)
 */
export enum ContentType {
	IMAGE = 'image',
	TEXT = 'text',
	DOCUMENT = 'document',
}
